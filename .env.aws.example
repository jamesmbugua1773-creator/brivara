# AWS Free Tier example env (use on EC2)
# Copy to .env.aws and fill values.

# URLs (replace with your real domains)
# Typical split:
# - Frontend (Hostinger): https://yourdomain.com
# - Backend API (AWS):    https://api.yourdomain.com
FRONTEND_URL=https://yourdomain.com
API_URL=https://api.yourdomain.com/api
WEBSITE_URL=https://yourdomain.com
CORS_ORIGINS=https://yourdomain.com
NEXT_PUBLIC_API_BASE=https://api.yourdomain.com/api

# Backend secrets (MUST be strong)
JWT_SECRET=replace_with_64+_chars
TRANSACTION_AUTH_SECRET=replace_with_64+_chars
WITHDRAWAL_PROVIDER_SECRET=replace_with_64+_chars

# Database (RDS) – use SSL
# Example:
# DATABASE_URL=postgresql://brivara_user:YOURPASS@brivara.xxxxxx.region.rds.amazonaws.com:5432/brivara_db?sslmode=require
DATABASE_URL=

# Optional Redis (only if you provision managed Redis)
# REDIS_URL=redis://:password@host:6379

# Email (recommended: SendGrid/Mailgun/SES)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@yourdomain.com

# Security / proxy
TRUST_PROXY=true

# Wallets / blockchain verification
WALLET_BEP20_ADDRESS=0xXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
WALLET_TRC20_ADDRESS=TXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
BSCSCAN_API_KEY=

# Withdrawals (keep disabled until provider integration is verified)
WITHDRAWAL_PROCESS_ENABLED=false
WITHDRAWAL_PROVIDER_URL=
